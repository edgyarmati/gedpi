{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'read -r input; file=$(echo \"$input\" | jq -r \".tool_input.file_path // .tool_response.filePath // empty\"); echo \"$file\" | grep -qE \"\\\\.(ts|tsx|js|json)$\" && (npm run check 2>&1; npx biome check \"$file\" 2>&1) || true'",
            "timeout": 30,
            "statusMessage": "Checking..."
          }
        ]
      }
    ]
  }
}
